simulator:
  states: ['ball_x', 'ball_y', 'ball_vel_x', 'ball_vel_y']
  actions: ["input_pitch","input_roll"]
  configs: {}
  episode_inits: {}
  initial_states:
  signal_builder:
  policy: bonsai
  logging: enable
  workspace_setup: True
