simulator:
  states:
    [
      "state_ball_x",
      "state_ball_y",
      "state_ball_vel_x",
      "state_ball_vel_y",
    ]
  actions: ["action_input_roll", "action_input_pitch"]
  configs: []
  episode_inits:
  policy: random
  logging: enable
  workspace_setup: True
