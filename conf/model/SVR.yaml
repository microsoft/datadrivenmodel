model:
  name: SVR
  build_params:
    model_type: SVR
    scale_data: True
    fit_separate: True
  saver:
    filename: models/moab/SVR_model
  sweep:
    run: False
    search_algorithm: bayesian
    num_trials: 3
    scoring_func: r2
